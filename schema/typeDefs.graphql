type User {
    _id: ID!
    firstName: String!
    lastName: String!
}

type Query {
    users: [User!]!
    findUserById(_id: String!): User!
}

type Mutation {
    addUser(firstName: String!, lastName: String!): User!
    deleteUser(_id: String!): User!
}